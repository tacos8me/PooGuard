NODE_ENV=test
PORT=3002

# Test database configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/clawguard_test
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=clawguard_test

# Redis (jest.mock'd in tests)
REDIS_URL=redis://localhost:6379

# JWT secret for testing
JWT_SECRET=test-jwt-secret-for-testing-only

# Model service (jest.mock'd in tests)
MODEL_SERVICE_URL=http://localhost:8000
